// jest-dom thêm các trình so khớp jest tùy chỉnh để xác nhận trên các nút DOM.
// cho phép bạn làm những việc như:
// mong đợi(element).toHaveTextContent(/react/i)
import '@testing-library/jest-dom';
